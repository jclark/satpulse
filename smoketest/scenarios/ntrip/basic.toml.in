# Ntrip caster serving RTCM from the packet log.
[serial]
device = "${SATPULSE_TEST_FIFO}"

[gps]
config = false
vendor = "u-blox"

[ntrip]
listen = "127.0.0.1:${SATPULSE_TEST_NTRIP_PORT}"

[[ntrip.mountpoint]]
name = "RTCM"
