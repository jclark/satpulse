# Ntrip caster mountpoint open to any authenticated top-level user.
[serial]
device = "${SATPULSE_TEST_SERIAL}"

[[user]]
name = "rover"
password = "secret"

[ntrip]
listen = "127.0.0.1:${SATPULSE_TEST_NTRIP_PORT}"

[[ntrip.mountpoint]]
name = "RTCM"
auth.anyUser = true
