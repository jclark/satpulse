# Event, track, and packet logging enabled.
[serial]
device = "${SATPULSE_TEST_FIFO}"

[gps]
config = false
vendor = "u-blox"

[log]
dir = "${SATPULSE_TEST_LOG_DIR}"
event = true
track = true
packet = true
