# HTTP endpoint with GUI and metrics disabled, position only.
[serial]
device = "${SATPULSE_TEST_FIFO}"

[[http]]
listen = "127.0.0.1:${SATPULSE_TEST_HTTP_PORT}"
gui = false
metrics = false
position = true
